import '../styles/globals.css'
export const metadata = { title: 'SwiftClean — INDIMOE Cleaning', description: 'SwiftClean — Cleaning business management portal' }
export default function RootLayout({ children }) {
  return <html lang="en"><body>{children}</body></html>
}
